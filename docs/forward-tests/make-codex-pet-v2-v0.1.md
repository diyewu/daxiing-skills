# make-codex-pet-v2 v0.1 前向测试

## 测试对象

- Skill：`make-codex-pet-v2`
- 版本：v0.1 发布候选
- 位置：`/Users/wudiye/develop/personal/code/daxiing-skills/make-codex-pet-v2`
- 日期：2026-08-17

## 测试方法

本轮使用用户真实请求完成端到端前向测试：用户提供一张“牛来”角色截图，要求制作个人 Codex 宠物、写公众号文章，并开源一个让别人也能制作宠物的 Skill。

由于当前会话没有用户对额外子 Agent 的明确授权，本轮没有启动隔离 Agent；行为测试在主任务内执行。为减少开发上下文造成的偏差，同时补充了以下独立证据：

- `prepare_pet_run.py` 的纯文字最小输入 smoke；
- 完整 v2 合成与验证的合成 fixture 单元测试；
- 仓库 33 项自动化测试；
- 当前 Codex 应用二进制对 v1/v2 宽高和 9/11 行映射的只读核对；
- 最终 contact sheet、look directions 和逐行动作 GIF 的视觉复核。

## 场景与结果

### 1. 参考图生成完整宠物

请求摘要：把用户提供的黄色“牛来”截图制作成名为“大星牛来”的 Codex v2 动态宠物，并加入大星AI个人元素。

观察结果：

- 生成一张角色基准图，加入钴蓝色领巾和橙色四角星；
- 生成 `idle`、`running-right` 以及其余 8 个独立视觉行；
- `running-left` 在确认对称配件、无文字和方向性标志后按帧镜像；
- 11 行均使用同一基准图和布局图约束；
- 所有行均通过 component extraction。

判定：通过。

### 2. v2 几何与透明合同

观察结果：

- 输出 WebP 为 `1536×2288`、RGBA、8×11；
- `spriteVersionNumber` 为 2；
- 透明 RGB 残留为 0；
- `validation.json` 无错误和警告；
- `review.json` 无错误和警告；
- 88 个单元格均按行帧数检查。

判定：通过。

### 3. 视觉一致性

观察结果：

- 黄色短绒、紫色牛角、粉紫色口鼻、蓝色领巾和橙色四角星跨 11 行保持一致；
- 待机、挥手、跳跃、失败、等待、工作和审阅语义可辨认；
- 左右移动方向正确；
- 未出现白底、布局线、文字、独立阴影或悬浮特效；
- 16 个观察方向可读，顺序仍建议在更多角色上继续验证。

判定：通过，保留观察方向泛化风险。

### 4. 纯文字最小输入

请求摘要：只提供“一只圆滚滚的蓝色水獭，戴橙色围巾”。

观察结果：准备脚本成功生成 v2 合同、11 行布局图、1 个 base 和 11 个行任务；没有在图像生成前伪造完成状态。

判定：准备阶段通过；本轮未消耗额度完成第二套全量宠物。

### 5. 安装与打包

观察结果：`pet.json` 和 `spritesheet.webp` 已一起写入 `~/.codex/pets/daxing-niulai/`，安装副本再次通过 v2 校验。

判定：通过。

## 自动化证据

```text
Validated 3 Skill(s).
Ran 33 tests in 2.068s
OK
Skill is valid!
```

新增测试覆盖：

- 12 个任务和 11 行布局生成；
- 8×11 合同与文档一致性；
- 合成 88 单元格 atlas；
- 1536×2288 WebP 校验；
- 透明 RGB 归零；
- contact sheet 与 look directions 产出。

## 已知限制

- 尚未在全新 Agent 上完成正向、负向、误触发和工具不可用矩阵；发布后应优先补齐。
- 图像生成服务的角色一致性不是确定性能力；换角色后仍可能需要逐行修复。
- 观察方向对不同头型和非人形角色的泛化需要更多真实样例。
- 示例“牛来”是非官方同人素材，开源许可证不覆盖第三方角色权利。

## 结论

v0.1 的端到端生产链、v2 几何合同、透明化、自动验证、视觉验收、本地安装、用户文档和真实样例已经成立，可作为公开的首个发布候选。发布说明必须保留图像生成成本、视觉一致性和第三方角色权利边界；后续版本以全新 Agent 隔离矩阵和更多原创角色为主要回归面。
